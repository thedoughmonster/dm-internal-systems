<!-- apps/web/README_COMPONENT_PARADIGM.md -->

# DM UI Component Paradigm

This file is a pointer and required reading.
The source of truth lives in `docs/AGENT_RULES_WEB_UI.MD`.

Lint enforces component import boundaries and route boundaries.
See `apps/web/eslint.config.mjs` and the helper in `apps/web/lib/eslint`.
